# Lab code dependencies for the AI Agents Workshop.
# MkDocs and documentation plugins are NOT listed here — they are installed
# as a global uv tool. See docs/README.md for the uv tool install command.
#
# Install lab dependencies with:
#   uv pip install -r requirements.txt
# Or, once pyproject.toml is in place:
#   uv sync

# TODO: add lab code dependencies here as labs are developed
